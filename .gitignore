node_modules/

# WhatsApp linked-device session (Signal protocol keys) — equivalent to a
# credential, never commit this.
auth_info/
auth_info*/

# Local state / audit DB once the moderation logic exists.
*.sqlite
*.sqlite3
.env
